[tox]
envlist = py33,py34

[testenv]
changedir = tests
deps      =
	discover
	taillight >= 0.2b3
commands  = discover

[pep8]
ignore          = E203,E502,E731
exclude         = PyIRC/numerics.py,.tox,doc,.git
max-line-length = 80
